#! /bin/zsh

# go path
export GOPATH="${HOME}/go"

# add go path
export PATH="${GOPATH}/bin:${PATH}"

# add home bin path
export PATH="${HOME}/bin:${PATH}"

# mac psql
#export PATH="/usr/local/opt/libpq/bin:$PATH"

# get-shit-done
export PATH="${HOME}/Documents/repos/get-shit-done:${PATH}"

export PATH="${HOME}/Documents/GoLand-2021.2.4/bin:${PATH}"
