# dotfiles

### Requirements

Set zsh as default shell:

```
chsh -s $(which zsh)
```

First time copy `.vim` manually to `$(HOME)/.vim`:

```
cp -r .vim "$HOME/.vim"
```

### Installing

```
make
```
